"use client";import {useState} from "react";import {useRouter} from "next/navigation";import {Check,Loader2} from "lucide-react";
export function ClientMediaReview({id}:{id:string}){const router=useRouter(),[feedback,setFeedback]=useState(""),[busy,setBusy]=useState(false),[error,setError]=useState<string|null>(null);async function save(){setBusy(true);setError(null);try{const r=await fetch("/api/media/"+id+"/review",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({feedback})});const d=await r.json().catch(()=>null);if(!r.ok||!d?.ok)throw new Error(d?.error||"Review was not confirmed.");router.refresh();}catch(e){setError(e instanceof Error?e.message:"Could not save review.");}finally{setBusy(false);}}return <div className="mt-3"><textarea value={feedback} onChange={e=>setFeedback(e.target.value)} maxLength={2000} rows={3} placeholder="Technique feedback and the next coaching cue…" className="w-full rounded-xl border border-divider bg-white p-3 text-base text-cream"/>{error&&<p role="alert" className="mt-2 text-xs text-status-limited">{error}</p>}<button disabled={busy||feedback.trim().length<2} onClick={()=>void save()} className="mt-2 inline-flex min-h-11 items-center gap-2 rounded-xl bg-sky px-4 text-sm font-semibold text-white disabled:opacity-50">{busy?<Loader2 className="h-4 w-4 animate-spin"/>:<Check className="h-4 w-4"/>}Finalize feedback</button></div>}
