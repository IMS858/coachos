"use client";
import {useState} from "react";import {Save} from "lucide-react";import {useRouter} from "next/navigation";
export function StaffOperationsEditor({staffId,status,owner,responsibilities}:{staffId:string;status:string;owner:string|null;responsibilities:string[]}){
 const router=useRouter(),[state,setState]=useState(status),[lead,setLead]=useState(owner??""),[duties,setDuties]=useState(responsibilities.join(", ")),[busy,setBusy]=useState(false),[message,setMessage]=useState<string|null>(null);
 async function save(){setBusy(true);setMessage(null);try{const response=await fetch("/api/admin/staff/operations",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({staff_id:staffId,staff_status:state,operational_owner:lead,responsibilities:duties.split(",").map(x=>x.trim()).filter(Boolean)})});const data=await response.json().catch(()=>null);if(!response.ok)throw new Error(data?.error||"Could not save staff state.");setMessage("Saved");router.refresh();}catch(cause){setMessage(cause instanceof Error?cause.message:"Could not save staff state.");}finally{setBusy(false);}}
 return <div className="mt-4 rounded-xl border border-divider p-3"><p className="text-xs font-semibold uppercase tracking-wide text-cream-faint">Operations</p><div className="mt-2 grid gap-2 sm:grid-cols-2"><label className="text-xs text-cream-dim">Staff status<select value={state} onChange={e=>setState(e.target.value)} className="mt-1 min-h-11 w-full rounded-xl border border-divider bg-white px-3 text-sm">{["active","onboarding","leave","inactive"].map(x=><option key={x}>{x}</option>)}</select></label><label className="text-xs text-cream-dim">Operational owner<input value={lead} onChange={e=>setLead(e.target.value)} placeholder="e.g. Jason" className="mt-1 min-h-11 w-full rounded-xl border border-divider px-3 text-sm"/></label></div><label className="mt-2 block text-xs text-cream-dim">Responsibilities<input value={duties} onChange={e=>setDuties(e.target.value)} placeholder="training, programming, follow-up" className="mt-1 min-h-11 w-full rounded-xl border border-divider px-3 text-sm"/></label><div className="mt-2 flex items-center gap-3"><button type="button" disabled={busy} onClick={save} className="inline-flex min-h-11 items-center gap-2 rounded-xl bg-sky px-3 text-sm font-semibold text-white disabled:opacity-50"><Save className="h-4 w-4"/>{busy?"Saving…":"Save operations"}</button>{message&&<p role="status" className="text-xs text-cream-dim">{message}</p>}</div></div>;
}
