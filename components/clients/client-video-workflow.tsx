import Link from "next/link";
import {Video,Upload,Library,MessageCircle} from "lucide-react";
import {ClientCoachingThread} from "@/components/media/client-coaching-thread";
export function ClientVideoWorkflow({clientId,clientName}:{clientId:string;clientName:string}){
 const firstName=clientName.split(" ")[0]||"this client";
 const tiles=[{href:"/capture?client_id="+clientId,title:"Record exercise video",detail:"Create an IMS demonstration for review.",icon:Video},{href:"/library?client_id="+clientId,title:"Reuse exercise demo",detail:"Start from reviewed exercise identity.",icon:Library},{href:"#form-video-review",title:"Review client videos",detail:"Play the submission and finalize client-visible feedback.",icon:Upload},{href:"/messages/"+clientId,title:"Start video conversation",detail:"Continue the coaching conversation.",icon:MessageCircle}];
 return <><section className="rounded-3xl border border-sky/20 bg-white p-5 shadow-sm"><p className="text-xs font-semibold uppercase tracking-widest text-sky">Video coaching</p><h2 className="mt-2 text-xl font-semibold text-cream">Connect the demonstration to {firstName}’s movement</h2><p className="mt-2 text-sm leading-6 text-cream-dim">IMS exercise demo → client program → client form video → coach feedback. Selecting an exercise or recording a video does not automatically publish a prescription.</p><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{tiles.map(tile=>{const Icon=tile.icon;return <Link key={tile.title} href={tile.href} className="rounded-2xl border border-divider bg-surface-soft p-4 transition hover:border-sky/50"><Icon className="h-5 w-5 text-sky"/><p className="mt-3 text-sm font-semibold text-cream">{tile.title}</p><p className="mt-1 text-xs leading-5 text-cream-dim">{tile.detail}</p></Link>;})}</div><p className="mt-3 text-xs leading-5 text-cream-faint">Live Coach OS Video Sessions are not launched yet. These tools are for recorded coaching and messaging.</p></section><ClientCoachingThread clientId={clientId}/></>;
}
