import Link from "next/link";
import { Video, Camera, Upload, MessageCircle } from "lucide-react";
export function ClientVideoWorkflow({clientId}:{clientId:string}){
 return <section className="rounded-2xl border border-divider bg-white p-5 shadow-sm"><div className="flex items-start gap-3"><div className="rounded-xl bg-sky/10 p-2.5"><Video className="h-5 w-5 text-sky"/></div><div><h2 className="text-lg font-semibold text-cream">Video coaching</h2><p className="mt-1 text-sm leading-6 text-cream-dim">Use video as part of the coaching record—not a separate app. Record demos for this client, review clips they send, and keep feedback connected to their program.</p></div></div><div className="mt-4 grid gap-3 sm:grid-cols-3"><a href="#coach-video" className="rounded-xl bg-surface-soft p-4"><Camera className="h-4 w-4 text-sky"/><p className="mt-2 text-sm font-semibold text-cream">Record a cue</p><p className="mt-1 text-xs text-cream-dim">Film from your phone after a session.</p></a><a href="#coach-video" className="rounded-xl bg-surface-soft p-4"><Upload className="h-4 w-4 text-sky"/><p className="mt-2 text-sm font-semibold text-cream">Upload a video</p><p className="mt-1 text-xs text-cream-dim">Use an existing demo or technique clip.</p></a><Link href={"/messages/"+clientId} className="rounded-xl bg-surface-soft p-4"><MessageCircle className="h-4 w-4 text-sky"/><p className="mt-2 text-sm font-semibold text-cream">Discuss it</p><p className="mt-1 text-xs text-cream-dim">Keep coaching feedback in the client thread.</p></Link></div><p className="mt-4 text-xs leading-5 text-cream-faint">Live video sessions are the next communications layer. They will use authenticated rooms rather than public meeting links.</p></section>;
}
