import Link from "next/link";
import {createClient} from "@/lib/supabase/server";
import {readCompleteEvidence} from "@/lib/migration/complete-read";
import {fuelDate,shiftDate,pendingCheckins,type Journal,type Review,type Checkin} from "@/lib/fuel/model";
type QueueData={pending:Journal[];names:Map<string,string>}|{error:true};
async function loadQueue():Promise<QueueData>{try{const db=await createClient(),{data:{user}}=await db.auth.getUser();if(!user)return {pending:[],names:new Map()};const from=shiftDate(fuelDate(),-27);const [entries,reviews]=await Promise.all([readCompleteEvidence<Journal>((a,b)=>db.from("fuel_journal_entries").select("id,client_id,kind,entry_date,revision,payload,plan_version_id,created_at",{count:"exact"}).eq("kind","weekly").gte("entry_date",from).order("id").range(a,b)),readCompleteEvidence<Review>((a,b)=>db.from("fuel_coach_reviews").select("id,client_id,entry_id,note,disposition,created_at",{count:"exact"}).order("id").range(a,b))]);const pending=pendingCheckins(entries,reviews),ids=[...new Set(pending.map(p=>p.client_id))];const q=ids.length?await db.from("profiles").select("id,full_name").in("id",ids):{data:[],error:null};if(q.error)throw Error();return {pending,names:new Map((q.data??[]).map(p=>[p.id,p.full_name]))};}catch{return {error:true};}}
export async function FuelReviewQueue(){const db=await createClient(),{data:{user}}=await db.auth.getUser();if(!user)return null;const me=await db.from("profiles").select("role,deleted_at").eq("id",user.id).maybeSingle();if(me.error)return <p role="alert" className="rounded-xl border border-divider p-3 text-sm">Fuel review authorization unavailable.</p>;if(!me.data||me.data.deleted_at||!["owner","trainer"].includes(me.data.role))return null;const data=await loadQueue();if("error" in data)return <section role="alert" className="rounded-2xl border border-divider bg-white p-4"><h2 className="font-semibold">Fuel check-ins unavailable</h2><p className="mt-2 text-sm text-cream-dim">The Fuel database rollout or evidence lookup is unavailable. No zero-action count was inferred.</p><Link href="/fuel" className="mt-2 inline-flex min-h-11 items-center text-sm font-semibold text-sky">Open Fuel & Performance →</Link></section>;const {pending,names}=data;return <section className="rounded-2xl border border-sky/20 bg-white p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-lg font-semibold">Fuel check-ins</h2><p className="mt-1 text-xs text-cream-dim">{pending.length} awaiting coach review · past 28 days · assigned clients only for trainers</p></div><Link href="/fuel" className="inline-flex min-h-11 items-center text-sm font-semibold text-sky">Fuel & Performance →</Link></div><div className="mt-3 space-y-2">{pending.slice(0,10).map(entry=><Link key={entry.id} href={`/clients/${entry.client_id}/fuel#fuel-checkins`} className="flex min-h-14 items-center justify-between gap-3 rounded-xl bg-surface-soft p-3"><div><p className="text-sm font-semibold">{names.get(entry.client_id)??"Assigned client"}</p><p className="mt-1 text-xs text-cream-faint">{entry.entry_date} · revision {entry.revision}</p></div><span className="text-xs font-semibold text-sky">{(entry.payload as Checkin).contact_requested?"Contact requested":"Review"}</span></Link>)}</div>{!pending.length&&<p className="mt-3 text-sm text-cream-dim">No unanswered fuel check-ins in this window.</p>}{pending.length>10&&<p className="mt-3 text-xs text-cream-faint">Showing the latest 10. All entries remain available in each client&apos;s workspace.</p>}</section>;}
