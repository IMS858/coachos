import test from "node:test";import assert from "node:assert/strict";import {reconcileMoneyEvidence} from "../lib/migration/money-reconciliation";test("displayed package value cannot become live balance without usage evidence",()=>{const [r]=reconcileMoneyEvidence([{source_id:"p",client_source_id:"c",kind:"package",amount_cents:108000,status:"outstanding",identity_resolved:true,usage_verified:false}]);assert.equal(r.state,"needs_review");assert.equal(r.reason,"package_usage_unverified");});test("transactions need settlement evidence and memberships require explicit recreation review",()=>{const r=reconcileMoneyEvidence([{source_id:"t",client_source_id:"c",kind:"transaction",amount_cents:73900,status:"deposited",identity_resolved:true,settlement_verified:false},{source_id:"m",client_source_id:"c",kind:"membership",amount_cents:73900,status:"active",identity_resolved:true}]);assert.equal(r[0].reason,"transaction_settlement_unverified");assert.equal(r[1].reason,"billing_recreation_requires_owner_review");});