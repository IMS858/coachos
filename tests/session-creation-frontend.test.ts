import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";import path from "node:path";const root=path.join(import.meta.dirname,"..");
test("new session frontend is training/assessment only and rotates retry identity when payload changes",()=>{const s=fs.readFileSync(path.join(root,"components/sessions/new-session-form.tsx"),"utf8");assert.match(s,/value: "training"/);assert.match(s,/value: "assessment"/);assert.doesNotMatch(s,/value: "massage"|value: "pilates"|value: "recovery"/);assert.match(s,/fingerprint/);assert.match(s,/crypto\.randomUUID/);});
test("standing booking frontend backend remains training only and origin checked",()=>{const s=fs.readFileSync(path.join(root,"app/api/recurring/route.ts"),"utf8");assert.match(s,/Standing bookings are training only/);assert.match(s,/Invalid request origin/);assert.match(s,/CAPTURE_UUID/);assert.match(s,/15–180 minutes/);});