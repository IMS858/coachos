const test=require("node:test"),assert=require("node:assert/strict"),fs=require("node:fs");test("today surfaces package state and session links close navigation loop",()=>{const d=fs.readFileSync("components/dashboard/trainer-dashboard.tsx","utf8"),s=fs.readFileSync("app/sessions/[id]/page.tsx","utf8");assert.match(d,/Package needs review/);assert.match(d,/left/);assert.match(s,/>Calendar</);assert.match(s,/>Client profile</);assert.match(s,/trainer=\$\{session\.trainer_id/)});