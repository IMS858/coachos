import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";import path from "node:path";const root=path.join(import.meta.dirname,"..");test("staff promotion is explicit owner-only audited and refuses real clients",()=>{const s=fs.readFileSync(path.join(root,"packages/db/migrations/0074_staff_identity_promotion.sql"),"utf8");assert.match(s,/Owner authorization required/);assert.match(s,/Client record cannot be promoted/);assert.match(s,/staff.profile_promoted/);assert.match(s,/ims.owner_profile_mutation/);});