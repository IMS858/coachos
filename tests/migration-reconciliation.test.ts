import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";import path from "node:path";const root=path.join(import.meta.dirname,"..");test("reconciliation remains owner-only and non-importing",()=>{const s=fs.readFileSync(path.join(root,"packages/db/migrations/0072_migration_reconciliation.sql"),"utf8");assert.match(s,/Owner authorization required/);assert.match(s,/Destination client not found/);assert.match(s,/migration.record_reconciled/);assert.doesNotMatch(s,/insert into public\.(clients|sessions|plans|payments)/i);});