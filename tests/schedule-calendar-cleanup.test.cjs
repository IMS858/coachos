const test=require("node:test"),assert=require("node:assert/strict"),fs=require("node:fs");const src=fs.readFileSync("app/schedule/page.tsx","utf8");test("schedule renders only selected trainer columns",()=>{assert.match(src,/\{visibleTrainers\.map\(\(t\) => \(/);assert.doesNotMatch(src,/\{trainers\.map\(\(t\) => \(\s*<div\s*key=\{t\.id\}\s*className="relative border-l/)});test("single trainer calendar is phone-friendly and date-aware",()=>{assert.match(src,/min-w-\[360px\]/);assert.match(src,/Selected day/);assert.match(src,/Pacific Time/)});