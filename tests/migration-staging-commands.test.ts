import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";import path from "node:path";const root=path.join(import.meta.dirname,"..");
test("migration staging command is owner-only idempotent and cannot import operations",()=>{const s=fs.readFileSync(path.join(root,"packages/db/migrations/0071_migration_staging_commands.sql"),"utf8");assert.match(s,/Owner authorization required/);assert.match(s,/Draft migration batch required/);assert.match(s,/Source record changed/);assert.match(s,/deduped_count/);assert.doesNotMatch(s,/insert into public\.(clients|sessions|plans|payments)/i);});
