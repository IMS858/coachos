import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";import path from "node:path";const root=path.join(import.meta.dirname,"..");
test("client form media has durable immutable review state",()=>{const sql=fs.readFileSync(path.join(root,"packages/db/migrations/0066_client_media_review.sql"),"utf8"),api=fs.readFileSync(path.join(root,"app/api/media/[id]/review/route.ts"),"utf8");assert.match(sql,/awaiting_review/);assert.match(sql,/coach_feedback/);assert.match(api,/assigned coach or owner/);assert.match(api,/already finalized/);assert.match(api,/recordAudit/);assert.doesNotMatch(api,/review_status:"awaiting_review"/);});
