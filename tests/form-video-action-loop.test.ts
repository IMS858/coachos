import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";import path from "node:path";const root=path.join(import.meta.dirname,"..");
test("client upload enters review queue and Action Center surfaces it",()=>{const upload=fs.readFileSync(path.join(root,"app/api/media/client-upload/route.ts"),"utf8"),action=fs.readFileSync(path.join(root,"app/action-center/page.tsx"),"utf8");assert.match(upload,/review_status: "awaiting_review"/);assert.match(action,/review_status","awaiting_review/);assert.match(action,/Client form videos/);assert.match(action,/Form videos/);});
