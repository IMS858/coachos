import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";import path from "node:path";const root=path.join(import.meta.dirname,"..");test("schedule dry-run persistence cannot create sessions",()=>{const s=fs.readFileSync(path.join(root,"packages/db/migrations/0073_migration_schedule_dry_run.sql"),"utf8");assert.match(s,/Ready appointments require resolved identities/);assert.match(s,/Owner authorization required/);assert.doesNotMatch(s,/insert into public\.sessions/i);});