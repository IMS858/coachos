import test from "node:test";import assert from "node:assert/strict";import {summarizeClassTimeSlots} from "../lib/classes/time-intelligence";import {readFileSync} from "node:fs";
test("class time intelligence groups recorded Pacific slots without recommending schedule changes",()=>{const rows=[{occurrence_id:"1",template_id:"t",trainer_id:"a",starts_at:"2026-09-22T00:00:00Z",capacity:4,status:"completed",name:"Mobility",category:"mobility",enrollments:[{client_id:"c",status:"attended"}]},{occurrence_id:"2",template_id:"t",trainer_id:"a",starts_at:"2026-09-29T00:00:00Z",capacity:4,status:"completed",name:"Mobility",category:"mobility",enrollments:[{client_id:"d",status:"waitlisted"}]}];const x=summarizeClassTimeSlots(rows)[0];assert.equal(x.occurrences,2);assert.equal(x.capacity,8);assert.equal(x.booked,1);assert.equal(x.waitlisted,1);});
test("attendance corrections require reasoned owner evidence and remain prelaunch",()=>{const migration=readFileSync("packages/db/migrations/0061_class_attendance_corrections.sql","utf8");assert.match(migration,/reason text not null/);assert.match(migration,/owner reads attendance corrections/);assert.match(migration,/No correction command or direct write grant is active during prelaunch/);assert.doesNotMatch(migration,/grant (?:insert|update|delete|all)/i);});
