import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";import path from "node:path";const root=path.join(import.meta.dirname,"..");
test("migration staging is owner-only and cannot directly mutate operational records",()=>{const sql=fs.readFileSync(path.join(root,"packages/db/migrations/0070_migration_staging.sql"),"utf8"),page=fs.readFileSync(path.join(root,"app/settings/migration/page.tsx"),"utf8");assert.match(sql,/migration_batches_owner_read/);assert.match(sql,/migration_records_owner_read/);assert.match(sql,/Writes are server-authorized only/);assert.doesNotMatch(sql,/insert into public\.(clients|sessions|plans|payments)/i);assert.match(page,/Vagaro stays read-only/);assert.match(page,/displayed package value is not customer debt/);});
