import test from "node:test";import assert from "node:assert/strict";import {clientContinuitySignals} from "../lib/coaching/continuity";
test("continuity signals surface recorded gaps without predicting churn",()=>{const rows=clientContinuitySignals("2026-09-25T12:00:00Z",{last_completed_at:"2026-09-01T12:00:00Z",next_session_at:null,package_remaining:1,draft_programs:1,active_programs:0,unreviewed_media:2});assert.ok(rows.some(x=>x.key==="gap"));assert.ok(rows.some(x=>x.key==="package"));assert.ok(rows.some(x=>x.key==="video"));assert.ok(rows.some(x=>x.key==="program"));assert.ok(rows.some(x=>x.key==="active-program"));assert.doesNotMatch(JSON.stringify(rows),/churn|probability|risk score/i);});
test("future booking suppresses schedule-gap action without hiding other evidence",()=>{const rows=clientContinuitySignals("2026-09-25T12:00:00Z",{last_completed_at:"2026-09-01T12:00:00Z",next_session_at:"2026-09-27T12:00:00Z",package_remaining:1,draft_programs:0,active_programs:1,unreviewed_media:0});assert.equal(rows.some(x=>x.key==="gap"),false);assert.equal(rows.some(x=>x.key==="package"),true);});
