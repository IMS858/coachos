import test from "node:test";import assert from "node:assert/strict";import {readFileSync} from "node:fs";
test("client plan closes the asynchronous video coaching loop",()=>{const plan=readFileSync("app/plan/page.tsx","utf8"),thread=readFileSync("components/media/client-coaching-thread.tsx","utf8");assert.match(plan,/ClipPlayback/);assert.match(plan,/SendToCoach/);assert.match(plan,/ClientCoachingThread/);assert.match(thread,/ClientMediaReview/);assert.match(thread,/coach_feedback/);});
test("program exercise demo verification cannot substitute another exercise video",()=>{const route=readFileSync("app/api/programs/assignments/[id]/video/route.ts","utf8");assert.match(route,/assignment\.data\.exercise_id!==exerciseId/);assert.match(route,/safety_status!==\"approved\"/);assert.match(route,/client_visible/);});
