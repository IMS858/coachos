import test from "node:test";import assert from "node:assert/strict";import {readFileSync} from "node:fs";
test("growth workspace promotes research without turning research into leads",()=>{const page=readFileSync("components/leads/growth-workspace.tsx","utf8"),workspace=readFileSync("lib/leads/workspace.ts","utf8");assert.match(page,/Research Desk/);assert.match(page,/General-population clients/);assert.match(page,/Premium-fit segment/);assert.match(workspace,/research candidates, NOT confirmed consumer leads/);assert.match(workspace,/do not infer any individual person's income/);});
test("client exercise set can surface saved prescription details",()=>{const source=readFileSync("components/clients/client-exercise-sets.tsx","utf8");assert.match(source,/sets/);assert.match(source,/reps/);assert.match(source,/rest/);assert.match(source,/tempo/);});
