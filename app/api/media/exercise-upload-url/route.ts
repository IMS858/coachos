import {type NextRequest,NextResponse} from "next/server";import {createClient,createServiceClient} from "@/lib/supabase/server";
export async function POST(request:NextRequest){const db=await createClient();const {data:{user}}=await db.auth.getUser();if(!user)return NextResponse.json({error:"Unauthorized"},{status:401});const me=await db.from("profiles").select("role,deleted_at").eq("id",user.id).maybeSingle();if(me.error)return NextResponse.json({error:"Authorization unavailable"},{status:503});if(!me.data||me.data.deleted_at||!["owner","trainer"].includes(me.data.role))return NextResponse.json({error:"Staff only"},{status:403});const b=await request.json().catch(()=>({}));const ext=String(b.ext??"mp4").toLowerCase().replace(/[^a-z0-9]/g,"");if(!["mp4","mov","webm"].includes(ext))return NextResponse.json({error:"Use an MP4, MOV or WebM video"},{status:400});const base=crypto.randomUUID();const path=`exercise-drafts/${user.id}/${base}.${ext}`;const svc=createServiceClient();const {data,error}=await svc.storage.from("client-media").createSignedUploadUrl(path);if(error||!data)return NextResponse.json({error:"Could not prepare exercise video upload"},{status:503});return NextResponse.json({path,token:data.token,signedUrl:data.signedUrl,storage_path:path});}
