/**
 * Build stamp. Rendered in the sidebar and on the login screen so it's possible
 * to tell at a glance which build is actually live — deploys here go through a
 * batched manual upload, and a stale cache looks identical to a failed deploy.
 */
export const BUILD_ID = "2026-07-26 · email-fix-1";
