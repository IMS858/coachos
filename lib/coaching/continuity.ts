export type ClientContinuityInput={last_completed_at:string|null;next_session_at:string|null;package_remaining:number|null;draft_programs:number;active_programs:number;unreviewed_media:number};
export type ContinuitySignal={key:string;label:string;detail:string;priority:"attention"|"next"};
export function clientContinuitySignals(now:string,input:ClientContinuityInput):ContinuitySignal[]{const out:ContinuitySignal[]=[];const nowMs=Date.parse(now);if(input.last_completed_at){const days=Math.max(0,Math.floor((nowMs-Date.parse(input.last_completed_at))/86400000));if(days>=14&&!input.next_session_at)out.push({key:"gap",label:"Reconnect / schedule",detail:days+" days since completed training with no next session booked.",priority:"attention"});}if(input.package_remaining!==null&&input.package_remaining<=2)out.push({key:"package",label:"Review package runway",detail:input.package_remaining===0?"Active package has no sessions remaining.":input.package_remaining+" sessions remain.",priority:"attention"});if(input.unreviewed_media>0)out.push({key:"video",label:"Review client video",detail:input.unreviewed_media+" client video"+(input.unreviewed_media===1?"":"s")+" awaiting coach review.",priority:"attention"});if(input.draft_programs>0)out.push({key:"program",label:"Finish program draft",detail:input.draft_programs+" draft program"+(input.draft_programs===1?"":"s")+" still private.",priority:"next"});if(input.active_programs===0&&input.last_completed_at)out.push({key:"active-program",label:"Review current programming",detail:"Training history exists without an active published program.",priority:"next"});return out;}
