export type MoneyEvidence={source_id:string;client_source_id:string|null;kind:"package"|"membership"|"transaction";amount_cents:number|null;status:string;usage_verified?:boolean;identity_resolved:boolean;settlement_verified?:boolean};
export type MoneyDecision={source_id:string,state:"ready"|"needs_review";reason:string|null};
export function reconcileMoneyEvidence(rows:MoneyEvidence[]):MoneyDecision[]{return rows.map(r=>{let reason:string|null=null;if(!r.identity_resolved)reason="client_identity_unresolved";else if(r.amount_cents===null||!Number.isInteger(r.amount_cents))reason="amount_needs_review";else if(r.kind==="package"&&!r.usage_verified)reason="package_usage_unverified";else if(r.kind==="membership")reason="billing_recreation_requires_owner_review";else if(r.kind==="transaction"&&!r.settlement_verified)reason="transaction_settlement_unverified";return{source_id:r.source_id,state:reason?"needs_review":"ready",reason};});}
